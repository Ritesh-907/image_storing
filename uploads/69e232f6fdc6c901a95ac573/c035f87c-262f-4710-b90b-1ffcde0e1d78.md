# ☕ Java OOP Masterclass

> A comprehensive, hands-on reference to **Object-Oriented Programming in Java** — covering classes, constructors, inheritance, polymorphism, abstraction, encapsulation, and more through real, runnable examples.

---

## 📚 Table of Contents

1. [Classes and Objects](#1-classes-and-objects)
2. [Constructors](#2-constructors)
3. [Overloaded Constructors](#3-overloaded-constructors)
4. [Static Methods and Fields](#4-static-methods-and-fields)
5. [Inheritance](#5-inheritance)
6. [Inheritance with `super()`](#6-inheritance-with-super)
7. [Method Overriding](#7-method-overriding)
8. [toString() Override](#8-tostring-override)
9. [Abstraction](#9-abstraction)
10. [Polymorphism](#10-polymorphism)
11. [Runtime Polymorphism](#11-runtime-polymorphism)
12. [Encapsulation with Getters and Setters](#12-encapsulation-with-getters-and-setters)
13. [Aggregation](#13-aggregation)

---

## 1. Classes and Objects

> **Concept:** A `class` is a blueprint. An `object` is an instance of that blueprint. Fields store state; methods define behavior.

```java
// Classes and Objects

class Car {

    String make = "Ford";
    String model = "Mustang";
    int year = 2025;
    double price = 58000.99;
    boolean isRunning = false;

    void start(){
        isRunning = true;
        System.out.println("You start the engine");
    }

    void stop(){
        isRunning = false;
        System.out.println("You stop the engine");
    }
}

class Main {

    public static void main(String[] args) {

        Car car = new Car();

        System.out.println("Is running : "+ car.isRunning);
        car.start();
        System.out.println("Is running : "+ car.isRunning);
        car.stop();
        System.out.println("Is running : "+ car.isRunning);

        // car.isRunning = false;
        // System.out.println(car.make);
        // System.out.println(car.model);
        // System.out.println(car.year);
        // System.out.println(car.price);
        // System.out.println(car.isRunning);
    }
}
```

---

## 2. Constructors

> **Concept:** A constructor is a special method that runs when an object is created. It initializes fields with provided values using the `this` keyword.

```java
// Constructor

class Car {

    String make;
    String model;
    int year;
    double price;
    boolean isRunning;

    Car(String make, String model, int year, double price){
        this.make = make;
        this.model = model;
        this.year = year;
        this.price = price;
        this.isRunning = true;
    }

    void start(){
        System.out.println("You start the "+ this.make + " " + this.model + " engine !");
    }

    void stop(){
        System.out.println("You stop the "+ this.make + " " + this.model + " engine !");
    }
}

class Main {

    public static void main(String[] args){

        Car car = new Car("Ford","Mustang",2025,89000.89);
        Car car2 = new Car("Tata","Punch",2024,500000);

        car.start();
        car.stop();

        System.out.println("------------");

        car2.start();
        car2.stop();
    }
}
```

---

## 3. Overloaded Constructors

> **Concept:** Constructor overloading allows a class to have multiple constructors with different parameter lists. Java picks the right one based on what arguments are passed.

```java
// Use case of Overloaded Constructor

class User {

    String username;
    String email;
    int age;

    User(){
        this.username = "Guest";
        this.email = "Not Provided";
        this.age = Integer.parseInt("0");
    }

    User(String username){
        this.username = username;
        this.email = "Not Provided";
        this.age = 0;
    }

    User(String username, String email){
        this.username = username;
        this.email = email;
        this.age = 0;
    }

    User(String username, String email, int age){
        this.username = username;
        this.email = email;
        this.age = age;
    }
}

class Main {

    public static void main(String[] args) {

        User user1 = new User("Ritesh");
        User user2 = new User("Sabin","sabin@gamil.com");
        User user3 = new User("Vivek","vivek@google.com",12);
        User user4 = new User();

        System.out.println(user1.username);
        System.out.println(user1.email);
        System.out.println(user1.age);

        System.out.println(user2.username);
        System.out.println(user2.email);
        System.out.println(user2.age);

        System.out.println(user3.username);
        System.out.println(user3.email);
        System.out.println(user3.age);

        System.out.println(user4.username);
        System.out.println(user4.email);
        System.out.println(user4.age);
    }
}
```

---

## 4. Static Methods and Fields

> **Concept:** `static` members belong to the **class itself**, not to any individual object. All instances share the same static field. Static methods can be called without creating an object.

```java
// Use case of static method

class Friends {

    static int noOfFriends;
    String name;

    Friends(String name){
        this.name = name;
        noOfFriends++;
    }

    static void showFriends(){
        System.out.println("You have " + noOfFriends + " total Friends");
    }
}

class Main {

    public static void main(String[] args) {

        Friends f1 = new Friends("Gaurav");
        Friends f2 = new Friends("Socrates");
        Friends f3 = new Friends("Sabin");
        Friends f4 = new Friends("Priyanshu");
        Friends f5 = new Friends("Naman");

        Friends.showFriends();
    }
}
```

---

## 5. Inheritance

> **Concept:** Inheritance lets a child class acquire fields and methods from a parent class using the `extends` keyword. It models an **"is-a"** relationship.

```java
// Inheritance

class Organism{

    boolean isAlive;

    Organism(){
        isAlive = true;
    }
}

class Plant extends Organism{

    void photsynthesis(){
        System.out.println("Plants absorb Sunlight!");
    }
}

class Animal extends Organism{

    void eat(){
        System.out.println("The Animal is eating");
    }
}

class Dog extends Animal{

    int lives = 1;

    void speak(){
        System.out.println("Dogs barks!");
    }
}

class Cat extends Animal{

    int lives = 9;

    void speak(){
        System.out.println("Cat Meow!");
    }
}

class Main {

    public static void main(String[] args) {

        Dog dog = new Dog();
        Cat cat = new Cat();
        Plant plant = new Plant();

        System.out.println(dog.isAlive);
        System.out.println(dog.lives);
        dog.eat();
        dog.speak();

        System.out.println(cat.isAlive);
        System.out.println(cat.lives);
        cat.eat();
        cat.speak();

        System.out.println(plant.isAlive);
        plant.photsynthesis();
    }
}
```

---

## 6. Inheritance with `super()`

> **Concept:** `super()` calls the parent class constructor from the child class. This lets child classes reuse parent initialization logic instead of duplicating it.

```java
// Inheritance and Use case of super()

class Person{

    String first;
    String last;

    Person(String first, String last){
        this.first = first;
        this.last = last;
    }

    void showName(){
        System.out.println(this.first + " " + this.last);
    }
}

class Student extends Person{

    double gpa;

    Student(String first, String last, double gpa){
        super(first, last);
        this.gpa = gpa;
    }

    void showGPA(){
        System.out.println("GPA : " + this.gpa);
    }
}

class Employee extends Person{

    double salary;

    Employee(String first, String last, double salary){
        super(first, last);
        this.salary = salary;
    }

    void showSalary(){
        System.out.println(this.first + "'s " + " Salary is " + this.salary);
    }
}

class Main {

    public static void main(String[] args) {

        Person p1 = new Person("Ritesh","Chaudhary");
        // p1.showName();

        Student s1 = new Student("Ritesh","Chaudhary",9.87);
        // s1.showGPA();

        Employee e1 = new Employee("Ritesh","Chaudhary",200000);
        e1.showSalary();
    }
}
```

---

## 7. Method Overriding

> **Concept:** A child class can provide its own implementation of a method defined in the parent class. The `@Override` annotation signals this intentionally. The child's version runs instead of the parent's.

```java
// Inheritance and Method @Overriding

class Animal{

    void move(){
        System.out.println("The Animal is Running");
    }
}

class Dog extends Animal{

    void speak(){
        System.out.println("Dogs barks!");
    }
}

class Cat extends Animal{

    void speak(){
        System.out.println("Cat Meow!");
    }
}

class Fish extends Animal{

    @Override
    void move(){
        System.out.println("The Animal is Swiming");
    }

    void speak(){
        System.out.println("Cat Meow!");
    }
}

class Main {

    public static void main(String[] args) {

        Dog dog = new Dog();
        Cat cat = new Cat();
        Fish fish = new Fish();

        dog.move();
        cat.move();
        fish.move();
    }
}
```

---

## 8. `toString()` Override

> **Concept:** Every Java object has a `toString()` method inherited from `Object`. Overriding it lets you control how an object prints as a human-readable string with `System.out.println()`.

```java
// Use case of toString

class Car {

    String make;
    String model;
    int year;
    String color;

    Car(String make, String model, int year, String color){
        this.make = make;
        this.model = model;
        this.year = year;
        this.color = color;
    }

    @Override
    public String toString(){
        return this.color + " " + this.year + " " + this.make + " " + this.model;
    }
}

class Main {

    public static void main(String[] args) {

        Car car = new Car("Ford","Mustang",2025,"Red");

        System.out.println(car);
    }
}
```

---

## 9. Abstraction

> **Concept:** Abstract classes define a **contract** — they declare methods without implementing them. Any concrete subclass *must* provide an implementation. You cannot instantiate an abstract class directly.

```java
// Abstraction

abstract class Shape{

    abstract double area();

    void display(){
        System.out.println("This is a Shape!");
    }
}

class Rectangle extends Shape{

    double width;
    double height;

    Rectangle(double width, double height){
        this.width = width;
        this.height = height;
    }

    @Override
    double area(){
        return height * width;
    }
}

class Triangle extends Shape{

    double base;
    double height;

    Triangle(double base, double height){
        this.base = base;
        this.height = height;
    }

    @Override
    double area(){
        return 0.5 * (base * height);
    }
}

class Circle extends Shape{

    double radius;

    Circle(double radius){
        this.radius = radius;
    }

    @Override
    double area(){
        return Math.PI * radius * radius;
    }
}

class Main {

    public static void main(String[] args) {

        Circle circle = new Circle(12);
        Rectangle rectangle = new Rectangle(12,56);
        Triangle triangle = new Triangle(14,13);

        circle.display();
        rectangle.display();
        triangle.display();

        System.out.println(circle.area());
        System.out.println(triangle.area());
        System.out.println(rectangle.area());
    }
}
```

---

## 10. Polymorphism

> **Concept:** Polymorphism means "many forms." The same method name (`go()`) behaves differently depending on which subclass calls it. Each class defines its own version of the method.

```java
// Polymorphism

abstract class Vechile{

    abstract void go();
}

class Car extends Vechile{

    @Override
    void go(){
        System.out.println("You dirve the car !");
    }
}

class Bike extends Vechile{

    @Override
    void go(){
        System.out.println("You ride the bike !");
    }
}

class Boat extends Vechile{

    @Override
    void go(){
        System.out.println("You sail the boat !");
    }
}

class Main {

    public static void main(String[] args) {

        Car car = new Car();
        Bike bike = new Bike();
        Boat boat = new Boat();

        car.go();
        bike.go();
        boat.go();
    }
}
```

---

## 11. Runtime Polymorphism

> **Concept:** The actual method that runs is determined **at runtime**, not at compile time. A parent-type reference variable can hold any subclass object, and the correct method is dispatched dynamically based on what the variable actually points to.

```java
// Runtime Polymorphism

import java.util.Scanner;

abstract class Animal {

    abstract void speak();
}

class Dog extends Animal{

    @Override
    void speak(){
        System.out.println("The dogs goes *woofs* ");
    }
}

class Cat extends Animal{

    @Override
    void speak(){
        System.out.println("The cats goes *meow* ");
    }
}

class Main {

    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        Animal animal;

        System.out.print("Would you like a Dog or Cat? (1 = Dog , 2 = Cat): ");
        int choice = scanner.nextInt();

        if(choice == 1){
            animal = new Dog();
            animal.speak();
        }
        else if(choice == 2){
            animal = new Cat();
            animal.speak();
        }
        else{
            System.out.println("Enter a Valid Option !");
        }
    }
}
```

---

## 12. Encapsulation with Getters and Setters

> **Concept:** Encapsulation protects object data by marking fields `private` and exposing controlled access through `get` and `set` methods. Setters can include validation logic to prevent invalid state.

```java
// Encapsulation using getter and setter

class Car{

    private final String model;
    private String color;
    private int price;

    Car(String model, String color, int price){
        this.model = model;
        this.color = color;
        this.price = price;
    }

    String getModel(){
        return this.model;
    }

    String getColor(){
        return this.color;
    }

    int getPrice(){
        return this.price;
    }

    void setColor(String color){
        this.color = color;
    }

    void setPrice(int price){
        if(price <= 0){
            System.out.println("Price can't be Zero !");
        } else {
            this.price = price;
        }
    }
}

class Main {

    public static void main(String[] args) {

        Car car = new Car("Punch","Yellow",600000);
        System.out.println(car.getColor() + " " + car.getModel() + " " + car.getPrice());

        car.setPrice(0);
        car.setColor("Pink");

        System.out.println(car.getColor() + " " + car.getModel() + " " + car.getPrice());
    }
}
```

---

## 13. Aggregation

> **Concept:** Aggregation is a **"has-a"** relationship between objects. One class contains a reference to another class. Unlike composition, the contained objects can exist independently.

```java
// Aggregation

class Book{

    String name;
    int page;

    Book(String name, int page){
        this.name = name;
        this.page = page;
    }

    void displayInfo(){
        System.out.println(this.name + " ( " + this.page + " pages )");
    }
}

class Library{

    int year;
    String name;
    Book[] books;

    Library(String name, int year, Book[] books){
        this.name = name;
        this.year = year;
        this.books = books;
    }

    void displayInfo(){
        System.out.println("The "+ this.year + " " + this.name);
        System.out.println("Books Available: ");
        for(Book book : books){
            book.displayInfo();
        }
    }
}

class Main {

    public static void main(String[] args) {

        Book book1 = new Book("The Fellow of the Ring",423);
        Book book2 = new Book("The Two Towers",352);
        Book book3 = new Book("The Return of the King",416);

        Book[] books = {book1, book2, book3};

        Library library = new Library("NYC Public Library",1897,books);

        library.displayInfo();
    }
}
```

---

## 🗂️ Topics Summary

| # | Topic | Key Concept |
|---|-------|-------------|
| 1 | Classes & Objects | Blueprint → Instance |
| 2 | Constructors | Object initialization with parameters |
| 3 | Overloaded Constructors | Multiple constructors, different signatures |
| 4 | Static Methods & Fields | Class-level state shared across instances |
| 5 | Inheritance | Child extends parent (`extends`) |
| 6 | `super()` | Child reuses parent constructor |
| 7 | Method Overriding | Child redefines parent method (`@Override`) |
| 8 | `toString()` | Custom string representation of objects |
| 9 | Abstraction | Abstract classes define contracts |
| 10 | Polymorphism | One method name, many behaviors |
| 11 | Runtime Polymorphism | Dynamic method dispatch at runtime |
| 12 | Encapsulation | Private fields + getters/setters |
| 13 | Aggregation | "Has-a" relationship between objects |

---

<div align="center">

Made with ☕ and Java

</div>
